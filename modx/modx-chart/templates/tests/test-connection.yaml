apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "modx-chart.fullname" . }}-test-connection"
  labels:
    {{- include "modx-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "modx-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
